---
name: work-order-flow
version: 1.8.0
last_updated: 2026-05-23
description: >-
  处理钉钉工单/聊天记录/零散需求的完整开发工作流。从接收工单、解析文档、分析需求、
  规划设计、执行迭代到生成日报的全流程管理。当用户提到工单、需求、bug修复、
  钉钉消息、日报、或发送PDF/Word文档时自动触发。
---

# 工单驱动开发工作流

## 概述

本工作流将钉钉工单从接收到交付的全过程标准化为 6 个阶段。
支持的输入类型：PDF工单、Word文档、纯文本、聊天记录截图、零散描述。

**每次回复时必须在开头标明当前阶段：**
```
📍 当前阶段：Phase X — {阶段名称}
```

**阶段转换规则（必须执行！）：**

当一个阶段的所有工作完成后，**必须主动告知用户并提示下一阶段**，格式：

```
✅ Phase X 已完成。
➡️ 下一步：Phase Y — {阶段名称}（简述该阶段要做什么）
是否继续？
```

**禁止**：完成一个阶段后沉默等待用户主动询问"下一步是什么"。工作流的阶段推进由 AI 主动驱动，用户只需确认或调整。

**阶段回退规则（非线性流程处理）：**

工作流**不是严格线性**的。用户可能在任何阶段发现问题需要回到前面的阶段，这是正常的。处理方式：

1. **识别回退** — 当用户在 Phase 5（收尾）提出"还有个小问题要改"或在 Phase 4.6（完整度检查）发现遗漏，识别这是**阶段回退**而非新需求
2. **轻量回退** — 回退到对应阶段处理问题，不需要重走整个流程：
   - 小 bug 修复 → 回到 Phase 4 修改 → L1 Review 该改动 → 回到原阶段继续
   - 需求理解偏差 → 回到 Phase 2 重新分析该模块 → Phase 4 修改 → 继续
3. **标注回退** — 在 progress.md 中记录回退事件：`| 时间 | 回退 Phase 4 | 原因：Phase 5 发现 XX 问题 | 修复完成 |`
4. **回退后的收敛** — 修复完成后，**不需要从头重走后续所有阶段**，只需对改动部分做 L1 Review，然后回到回退前的阶段继续

```
正常流程：Phase 0 → 1 → 2 → 3 → 4 → 4.5 → 4.6 → 5

回退示例：
Phase 5 中发现 bug → 回退 Phase 4（修复+L1 Review）→ 回到 Phase 5 继续
Phase 4.6 发现遗漏模块 → 回退 Phase 4（实现+L1 Review）→ 回到 Phase 4.6 继续
```

> **核心思想：** 工作流是一个有主干的图，不是一条直线。回退是轻量的"局部循环"，不是"从头再来"。

```
Phase -1   会话恢复    ─→  续接已有需求时，读 spec.md + progress.md 恢复上下文
    │
Phase 0    接收识别    ─→  识别输入类型，提取工单信息
    │
Phase 1    解析文档    ─→  将 Word/PDF 转为可读 Markdown + 读图（Ingest）
    │
Phase 2    分析验证    ─→  分析需求 + 工单评论变更 + 交叉验证 + 收集上下文
    │
Phase 3    规划设计    ─→  创建 spec.md + progress.md，复杂需求做 Spec Plan
    │
Phase 4    执行迭代    ─→  逐模块：回读需求(Query) → 编码 → L1 Review → 留痕
    │                  ↑ （回退：任何后续阶段发现问题都可回到这里修复）
Phase 4.5  三级Review  ─→  L1模块级 → L2全局自检(Lint) → L3用户验收
    │
Phase 4.6  完整度检查  ─→  需求-实现全面对照，查漏补缺
    │
Phase 5    收尾输出    ─→  日报 + 归档 + 交付总结文档
```

---

## Phase -1: 会话恢复（续接已有需求时）

当在**新对话**中继续一个已有需求时（如用户说"继续昨天的PHC需求"、"上次那个工单"），不要从零开始，先恢复上下文：

1. **读取 progress.md** — 了解上次做到哪里、什么状态、有哪些待解决问题
2. **读取 spec.md** — 回顾代码→文档映射表、任务拆解、风险点
3. **读取决策记录** — 了解之前做过哪些关键决策、用户确认过什么
4. **确认当前阶段** — 根据 progress.md 的最后记录，确定当前处于哪个 Phase
5. **向用户简要复述** — "上次我们完成了……，待解决的问题是……，现在继续从……开始？"

> **核心原则：** spec.md 和 progress.md 就是 AI 的"记忆"。新会话不需要从零理解需求，读文档即可恢复。这也是留痕的核心价值——不止为人服务，也为下一次对话的 AI 服务。

### 对话压缩后的上下文确认（关键！）

当对话内容被平台**自动压缩/摘要**后（如 Cursor 的对话摘要、Claude Code 的上下文截断等），AI 丢失了压缩前的完整上下文。此时**禁止直接按新消息执行**，必须先确认：

1. **识别压缩事件** — 如果当前对话存在摘要信息（如 `[Previous conversation summary]`）、或对话开头出现了大量上下文而非正常的逐步交流，说明发生了对话压缩
2. **回顾压缩前的工作焦点** — 从摘要中提取：压缩前在做什么任务？是代码开发还是工作流优化？当前阶段是什么？
3. **与新消息对比** — 新消息的主题与压缩前的工作焦点是否一致？
4. **不一致时主动确认** — 如果检测到焦点切换（如压缩前在优化工作流，新消息在问代码问题），**先向用户确认**：

```
我注意到对话刚做了一次压缩。压缩前我们在{之前的任务}，你现在发的是{新消息的主题}。
请确认：是继续{之前的任务}，还是切换到{新任务}？
```

5. **一致时直接继续** — 如果新消息与压缩前焦点一致，正常继续，无需额外确认

**反模式（禁止）：**
- ❌ 压缩后不看摘要，直接按新消息的字面意思行动
- ❌ 压缩前后焦点不同却不确认，导致用户困惑

> **核心思想：** 对话压缩就像"失忆后醒来"——先搞清楚之前在做什么，再决定下一步。不确认就行动，等于失忆后乱跑。

### 容错机制：文档不可靠时的处理

文档可能过时、缺失或不完整。**AI 必须自我检测文档可信度，不能盲目信任。**

#### 可信度检查清单

| 检查项 | 判断方式 | 不通过时的处理 |
|-------|---------|-------------|
| progress.md 存在 | 尝试读取 | 不存在 → 降级到 Phase 0 从头开始 |
| progress.md 时效性 | 最后更新日期 vs 当前日期 | 超过 7 天 → 标记"可能过时"，向用户确认 |
| spec.md 存在 | 尝试读取 | 不存在 → 仅靠 progress.md + 代码推断 |
| 待解决问题列表一致性 | progress.md 的待办 vs 代码实际状态 | 不一致 → 以代码实际状态为准，记录差异 |
| 决策记录完整性 | DR 编号是否连续、内容是否清晰 | 有跳号/模糊 → 标记为"上下文缺失" |

#### 降级策略（逐级）

```
Level 1: 文档完整且新鲜 → 直接恢复，简要复述后继续
Level 2: 文档存在但可能过时 → 恢复 + 用代码/git log 交叉验证 → 向用户确认关键点
Level 3: 仅有部分文档 → 读有的 + git log 补充 → 列出"不确定项"让用户确认
Level 4: 文档全部缺失 → 回退到 Phase 0，但先检查代码和 git log 推断进度
```

#### 交叉验证手段

当文档可信度不足时，用以下手段补充：

1. **git log** — 查看最近提交，推断做了什么、做到哪里
2. **代码实际状态** — 读关键文件，看功能是否已实现
3. **工单/需求原文** — 对比 spec.md 的任务拆解，看是否遗漏
4. **直接问用户** — "上次做到了 XX，对吗？还有哪些需要调整？"

> **底线原则：** 宁可多问用户一句确认，也不要基于过时文档做错误决策。如果对恢复的上下文有任何不确定，**必须明确告知用户哪些是确定的、哪些是推断的、哪些需要用户确认**。

---

## Phase 0: 接收识别

### 0.1 确认关注层

需求通常涉及多个层级（终端→后端→前端），但用户可能只关注其中一部分。
在分析需求前，先确认用户的关注范围：

| 关注层 | 适用场景 | 分析重点 |
|-------|---------|---------|
| **前端** | 页面开发、UI调整 | 前端组件、页面路由、API 对接 |
| **后端** | 接口开发、数据处理 | Controller、Service、数据库 |
| **全栈** | 端到端新功能 | 前后端都分析，注意接口对齐 |
| **未指定** | 默认 | 主动询问，或根据工作区代码判断 |

如果用户未指定，根据当前工作区的代码类型自动判断（如工作区只有前端代码则聚焦前端），并告知用户。

### 0.2 输入识别

当用户发来任何内容时，判断输入类型：

| 输入类型 | 识别特征 | 处理方式 |
|---------|---------|---------|
| **正式工单** | 含工单编号（如 `2026XXXX...`）、PDF附件 | 完整走 Phase 0-5 |
| **Word 需求文档** | .docx 文件 | 解析后走 Phase 1-5 |
| **聊天记录/口头描述** | 零散文字、截图、"帮我改一下XX" | 简化流程，Phase 2→4→5 |
| **纯 Bug 修复** | "XX功能报错"、"XX显示不对" | 快速定位，Phase 2→4→5 |

**操作步骤：**
1. 识别输入中的工单编号（如有），记录到需求文档
2. 判断是否有附件需要解析（Word/PDF）
3. 如果用户发的信息零散模糊，主动整理并确认："我理解你的需求是……，对吗？"

> **容错原则：** 用户可能直接甩来零散信息，不要要求格式化输入。
> 主动从混乱信息中提取关键要素：**做什么、改哪里、期望效果、工单编号**。

---

## Phase 1: 解析文档

如果有 Word 或 PDF 附件，使用解析脚本转为 Markdown。

**解析脚本位置：** `.cursor/skills/work-order-flow/scripts/parse_doc.py`

```bash
# 解析 Word 文档（含图片提取）
python .cursor/skills/work-order-flow/scripts/parse_doc.py input.docx -o output_dir/

# 解析 PDF 文档
python .cursor/skills/work-order-flow/scripts/parse_doc.py input.pdf -o output_dir/
```

脚本会输出：
- `content.md` — 文档正文转为 Markdown
- `images/` — 提取的图片文件（Word 中嵌入的图片）

如果解析效果不佳（尤其是复杂表格或扫描版 PDF），告知用户并请求手动补充关键信息。

### 1.2 图片阅读

解析后的 `images/` 文件夹中的图片**必须主动阅读**（使用 Read 工具直接打开图片文件）。

需求文档中的图片通常包含关键信息：
- **功能树状图** — 展示模块层级和功能点枚举，比文字描述更准确
- **数据协议图** — 列出字段名称和层级关系，直接指导数据模型设计
- **UI 原型图/截图** — 展示页面布局和交互逻辑
- **架构图** — 展示系统/模块之间的关系

**执行要求：**
1. 文档解析完成后，**逐张阅读**所有提取的图片
2. **确保图片不遗漏** — 解析脚本可能遗漏表格内嵌图片。解析完成后，用脚本核对 Word 文档中的图片总数与已提取图片数量，补提缺失图片
3. 将图片中的信息整合到需求分析中（如字段列表、模块结构等）
4. 如果图片与文字描述有出入，进行询问
5. **注意参考截图 vs 实际需求** — 需求文档中的 UI 截图可能截自其他已上线工艺的页面，仅作为参考样式，并不代表新工艺要完全按截图实现。区分：
   - 功能树状图/架构图 → 直接使用，表示新工艺的实际功能结构
   - UI 截图 → 参考样式和布局，但字段和细节按需求文字描述和当前工艺特性调整
6. 在后续实现阶段需要核实字段名、模块结构时，**回头翻阅相关图片**

> Read 工具支持的图片格式：jpeg/jpg、png、gif、webp

---

## Phase 2: 分析验证

**这是最关键的阶段。** 不要急于动手，先确保理解正确。

### 2.1 需求提取

从工单/文档中提取：
- **功能描述**：需要做什么
- **涉及模块**：哪个子系统、哪些页面
- **验收标准**：怎样算完成
- **工单编号**：用于日报和追踪

### 2.2 工单评论检查

**需求文档不是唯一真相来源。** 工单流程中的评论经常包含对需求文档的临时修改、补充说明、甚至推翻原有方案的决定。

**必须在读完需求文档后、动手实现前，逐条阅读工单评论：**

1. **获取工单评论** — 工单通常以 PDF 形式提供，评论位于工单末尾（"添加评论"区域）。如果工单已解析为 Markdown，在文件末尾查找以 `XXX 添加评论 YYYY-MM-DD` 格式出现的内容
2. **识别变更指令** — 评论中可能包含：
   - 字段名称变更（如"最终入土下压深度改为打入累计桩长"）
   - 格式/单位修改（如"焊接时长格式改为Xmin"）
   - 功能增删（如"数据报告此次暂不上线"）
   - 新增附件或参考文档
3. **评论优先级高于需求文档** — 当评论与需求文档描述冲突时，以评论（尤其是最新评论）为准
4. **区分有效评论和讨论** — 注意区分：
   - 确认性回复（"收到"、"已确认"）→ 表示前序变更已确认
   - 撤回评论 → 被撤回的内容无效，但可能被重新发布
   - @具体开发人员的指令 → 直接相关
   - 一般讨论（进度询问等）→ 非需求变更
5. **记录变更清单** — 将评论中提取的需求变更整理为清单，在后续实现阶段对照执行

### 2.3 交叉验证

> 工单内容不一定正确，产品可能没完全理解业务。

- 对照现有代码验证需求描述是否与系统现状一致
- 检查需求是否存在矛盾或模糊之处
- **别字校对**：需求文档中的专业术语可能存在错别字（如"施工回访"实为"施工回放"），对照代码中的实际命名进行纠正
- 如果发现问题，整理后反馈给用户确认

### 2.4 上下文收集

**先自查工作区，再问用户要。**
1. 主动搜索工作区中的后端/前端代码，查找相关接口和实现
2. **查看相关代码库的最近 2-3 次 git 提交**（仅相关库，不要查无关项目的提交）——这些提交可能包含与当前需求直接相关的改动上下文
3. 查看是否有参照模块的代码可以复用
4. 整理已掌握和缺失的信息后，才向用户索要剩余部分

```
我需要以下信息来完成这个需求：
1. ✅ 已有：[列出已掌握的信息，包含自己从代码中发现的]
2. ❓ 缺失：[列出需要补充的信息]
   - 工作区代码中未找到的接口？
   - 涉及的数据库表结构？
   - 是否有参考的已有实现（如其他项目的代码）？
   - 部署环境信息？
```

---

## Phase 3: 规划设计（复杂需求）

**触发条件：** 涉及多文件改动、新模块开发、或预估工作量 > 2小时的需求。
简单 bug 修复或小调整可跳过此阶段。

### 3.1 创建需求文件夹

在 `docs/` 下创建以工单编号或需求名命名的文件夹：

```
docs/
└── 需求-{简短描述}-{工单编号}/
    ├── spec.md          # 需求规格与实现计划
    ├── progress.md      # 进度追踪
    └── notes.md         # 过程笔记（可选）
```

### 3.2 扫描工作区 & 代码库映射

**在编写 Spec 前，必须先扫描工作区中所有代码库**，建立「需求模块 → 代码库路径」的映射表。

示例格式：
```
| 需求模块 | 代码库/路径 | 类型 |
|---------|-----------|------|
| 实时监控 | D:\xxx\pudong4 | 前端 Vue |
| 数字沙盘 | D:\xxx\board_pd4 | 前端 Vue |
| 后端接口 | D:\xxx\java\phc | Java Spring Boot |
```

这样用户能清楚知道改动涉及哪些代码库，避免遗漏。

### 3.3 编写 Spec（参考 spec_driven_develop 思想）

使用模板（位于本 Skill 的 `templates/spec.md`）

Spec 核心内容：
1. **需求概述** — 一句话说清楚要做什么
2. **代码库映射** — 哪个需求改哪个库
3. **现状分析** — 当前系统相关部分的状态
4. **实现方案** — 具体的技术方案和步骤
5. **影响范围** — 改动涉及哪些文件/模块
6. **风险点** — 可能踩的坑
7. **任务拆解** — 按步骤拆解为可执行的小任务

### 3.4 轻量留痕

**每个需求必须留痕，但不能过重。** 核心原则：**重要信息不丢失，但不占用过多上下文。**

在需求文件夹中维护以下文档：

```
docs/需求名/
├── spec.md          # 需求分析和实现计划（Phase 3 输出，之后不频繁更新）
└── progress.md      # 进度追踪（Phase 4 持续更新）
```

#### 设计理念（参考 Karpathy LLM Wiki 三层架构）

> "Obsidian 是 IDE，LLM 是程序员，Wiki 是代码库。"

核心思想：知识不应该在每次对话时从零理解，而应该被 AI **增量编译为结构化文档**，形成持久的、复利式的知识制品。维护簿记是 AI 最擅长的——更新映射、保持记录一致、追踪决策，**维护成本趋近于零**。

**三层映射到我们的工作流：**

```
docs/需求名/
├── parsed/          # 第一层 Raw：需求原文、解析文档、提取的图片（只读，不修改）
├── spec.md          # 第二层 Wiki：需求分析、代码映射、任务拆解（Phase 3 创建）
├── progress.md      # 第二层 Wiki：追加式日志 + 决策记录（Phase 4 持续更新）
└── (parsed_pdf/)    # 第一层 Raw：工单 PDF 解析产物（只读）

# 第三层 Schema = 本 SKILL.md（工作流规范，人+AI 共同演化）
```

| Karpathy 模式 | 工单工作流对应 | 说明 |
|--------------|---------------|------|
| Raw（原始来源） | parsed/ 目录、工单 PDF | 只读真相来源，AI 不修改 |
| Wiki（编译产物） | spec.md + progress.md | AI 维护的结构化知识 |
| Schema（规范） | SKILL.md | 告诉 AI 如何工作 |
| Ingest | Phase 1-2（解析+分析） | 新需求 → 编译进 spec |
| Query | Phase 4.1.1（回读需求） | 执行时查阅已编译知识 |
| Lint | Phase 4.5-4.6（Review+完整度检查） | 定期健康检查 |

#### spec.md（内容导向）

spec.md 在 Phase 3 创建，记录"是什么"和"怎么做"：

1. **需求概述** — 一句话说清楚
2. **代码→文档映射表**（核心！）— 改了哪个代码对应哪个需求模块
3. **交叉验证发现** — 工单 vs 需求差异、别字、评论变更
4. **任务拆解** — 按步骤的执行计划
5. **风险点** — 可能踩的坑

**代码→文档映射表** 示例：
```markdown
| 代码路径 | 需求模块 | 需求章节 |
|----------|---------|---------|
| src/views/control/PHCPile/Monitor.vue | 实时监控 | content.md "实时监控" |
| src/views/control/manager/cellset/PHCPileCellSet.vue | 单元管理 | content.md "单元管理" |
| board_pd4/src/js/dealJs/dics.js | 数字沙盘 | content.md "数字沙盘" |
```

> **映射表的作用：** 当修改某个代码文件时，能快速定位到对应的需求章节回读。类似 wiki-maintenance.md 中的代码→文档映射。

#### progress.md（时间导向）

progress.md 是追加式操作日志，记录"何时做了什么"：

```markdown
### YYYY-MM-DD

| 时间 | 阶段 | 内容 | 结果 |
|------|------|------|------|
| HH:MM | Phase X | 做了什么 | 完成/部分/阻塞 |

## 待解决问题
- [ ] 未完成项（随时更新，完成后标 [x]）

## 决策记录
| # | 决策 | 理由 | 日期 |
|---|-----|------|------|
| DR-001 | 为什么这样做 | 因为什么 | MM-DD |
```

**决策记录（DR-XXX）** 重点记录：
- 有歧义时用户怎么确认的
- 为什么跳过某功能、为什么选某方案
- 用户反馈引起的变更
- 工单评论与需求文档的冲突及裁决

#### 什么必须记（Ingest 原则）

1. **决策和理由** — 后续对话的 AI（或人）需要知道的上下文
2. **阶段性成果** — 每完成一个模块/Review，记录一行
3. **踩坑和反直觉修复** — 避免重复踩坑
4. **工单评论变更** — 与原始需求的差异
5. **用户反馈引起的变更** — 用户说"不对"后改了什么

#### 什么不用记

- 常规 lint 修复、每行代码改动（git diff 可以看）
- 工具调用过程

#### 更新策略：事件驱动，增量实时

**核心原则：不等阶段结束才批量更新，有变化就立刻更新。**

文档更新由**事件**触发，而非阶段边界触发。以下事件发生时，**立即**更新对应文档：

| 触发事件 | 更新动作 | 更新文件 |
|---------|---------|---------|
| 完成一个代码改动（如修了一个 bug、改了一个 API 路径） | 追加一行执行记录 | progress.md |
| 用户确认一个决策（如"64 先不改"、"这个字段不需要"） | 追加一条决策记录 | progress.md |
| 发现一个新的待解决问题 | 添加到待办列表 | progress.md |
| 解决了一个待办问题 | 标记为已完成 | progress.md |
| 代码文件与需求模块的映射关系变化 | 更新映射表 | spec.md |
| Review 发现问题并修复 | 记录发现和修复 | progress.md |
| 对话即将结束 | 确保所有变化已记录、待办列表是最新的 | progress.md |

**关键区别：**
- ❌ 旧模式：Phase 4 做完 → 批量更新 progress.md
- ✅ 新模式：改了一个文件 → 立刻追加一行；做了一个决策 → 立刻记录

**实操建议：**
- 如果当前操作很小（改一行代码），可以在当前回复中顺带更新文档，不需要单独开一轮
- 如果连续做了多个小改动，可以在最后一个改完后一次性补记，但**不能跨用户消息延迟**——用户下一条消息来之前必须已经记录完
- 决策记录是最重要的，因为它记录了"为什么"，而代码只记录了"是什么"

> **底线：** 如果一个新对话来了，只靠 progress.md + spec.md + 代码就能恢复 80% 的上下文，那文档就是合格的。如果做不到，说明更新不够及时。

> **禁止行为：** 不编造未实现的功能，不遗漏关键决策，不丢失用户反馈，不删除已确认的内容。
>
> **远景规划：** 当前采用本地 MD 留痕。未来可对接 Obsidian（作为浏览 IDE）+ CLI 工具（自动化索引/链接检查），实现完整的 LLM Wiki 模式。

### 3.5 确认方案

将 Spec 展示给用户确认后再进入执行阶段。

---

## Phase 4: 执行迭代

### 4.1 按计划执行

- 按 Spec 中拆解的任务逐个执行
- **每完成一个子任务或阶段性节点**，追加一行到 `progress.md`（时间、阶段、内容、结果）
- 关键决策和用户反馈引起的变更，记入决策记录
- 遇到阻塞或方案偏差，及时反馈

### 4.1.1 细粒度上下文回读（关键！）

**全局规划时看全局，局部执行时看局部。**

在着手实现**每一个具体模块/功能**前，必须执行以下"回读"动作：

1. **回读需求文档中该模块的对应章节** — 不要凭高层理解动手，重新定位到需求文档中描述该模块的段落，逐字阅读字段名称、单位、格式、小数位、显示/隐藏规则等细节
2. **回读工单评论中与该模块相关的变更** — 检查是否有针对该模块的临时修改（Phase 2.2 中整理的变更清单）
3. **回读相关图片** — 如果需求文档中有该模块的 UI 截图或数据表格图，重新打开查看细节
4. **对照写代码** — 带着具体的字段/格式要求去写代码，而不是凭记忆

**反模式（禁止）：**
- ❌ 高层扫一遍需求就开始写所有模块
- ❌ 凭"大概记得需求说了什么"来实现细节
- ❌ 只在用户说"不对"之后才回去看需求文档

**正确模式：**
- ✅ 实现"实时监控"前 → 回读需求文档中"实时监控"章节 + 相关表格 + 工单评论
- ✅ 实现"数据报表"前 → 回读需求文档中"数据报表"章节 + 工单评论
- ✅ 每个字段的标签、单位、格式都对照需求确认

> **核心思想：** 规划阶段的抽象理解足以拆任务，但实现阶段的细节只有回读原文才能保证准确。
> 花 30 秒回读需求，胜过花 30 分钟返工。

### 4.2 安全守则

- **数据库写操作**（INSERT/UPDATE/DELETE/ALTER/DROP）必须先展示 SQL 给用户确认
- **危险命令操作** 必须先说明影响再执行
- **git commit/push** 需要用户明确许可

### 4.3 迭代验证

每轮改动后：
1. 检查是否引入了 lint 错误
2. 验证改动是否符合预期
3. 如果有偏差，记录原因并调整

### 4.4 上下文对齐原则

当用户反复反馈"还是不对"或"怎么还没改好"时，**不要被情绪带偏盲目修改**，先执行以下检查：

1. **确认改动范围一致** — 你改的是 A 项目，用户测的是不是也是 A 项目？有没有在测别的环境或分支？
2. **确认环境一致** — 前端是否热加载了？后端是否重新部署了？本地 vs 线上环境是否一致？
3. **确认理解一致** — 回顾对话上下文，你和用户说的是不是同一个东西？颗粒度是否对齐？
   - 例：用户说"格式不对"，可能指的是A格式，但你理解的是B格式
   - 例：用户说"接口不对"，可能是代理/网关问题，不是代码问题
4. **先分析再行动** — 不要收到"不对"就立刻改代码，先用事实确认问题：
   - 检查网络请求的实际 URL 和返回值
   - 检查你的改动是否已生效（文件是否保存、服务是否重启）
   - 对照代码逻辑推理，确认自己的改动是否正确
5. **清晰复述** — 如果分析后确认自己的改动是正确的，向用户清晰说明原因，而不是盲目回滚

> **核心思想：** 反复拉扯往往不是代码问题，而是双方的上下文没对齐。
> 先对齐上下文，再解决问题。

### 4.5 三级 Review 机制

开发不是"写完就完"，每个模块和整体都需要经过 Review 才能进入下一步。

#### Level 1：模块级 Review（每完成一个模块）

每完成一个具体模块（如实时监控、数据报表、单元管理等），立即对该模块做一次局部 Review：

1. **回读需求** — 重新打开需求文档中该模块的章节 + 工单评论中的相关变更
2. **逐项核对** — 对照需求列出的字段/功能，检查实现是否完整：
   - 字段名称、标签是否与需求一致
   - 单位、格式、小数位是否正确
   - 显示/隐藏逻辑是否符合要求
   - API 路径是否正确
3. **移植残留扫描（关键！）** — 如果代码是从其他工艺/模块**复制移植**而来，必须执行机械化扫描：
   - **搜索源工艺的所有标识符** — 对改动文件用 grep 搜索源工艺的关键字（如工艺缩写 XPZ/JBZ/SSZ、中文名 旋喷桩/搅拌桩、API 路径前缀 xpzMap/jbzMap、GeoServer 工作空间名、CSS 类名、下载文件名等）
   - **每个匹配项逐一判定** — 是否需要替换为目标工艺的对应值
   - **查后端确认** — API 路径不能靠"看别人也在用所以不用改"来判断，必须去看目标工艺的后端 Controller 确认实际路径
   - **禁止基于模式匹配的推断** — "40 个文件都用 /xpzMap/ 所以它是公共服务"是典型的错误推断。正确做法：去后端看有没有 `/phcMap/` 对应的 Controller
4. **Lint 检查** — 确保无编译/类型错误
5. **确定性分级处理** — Review 中发现的问题，按确定性分级处理：
   - **确定的问题**（如明显的拼写错误、漏加单位）→ 直接修复，修复后告知用户
   - **不确定/有歧义的问题**（如某个 kind 值可能有特殊含义、某个字段可能由后端控制）→ **先思考上下文，仍拿不准则询问用户**，不要贸然修改
   - **核心原则：** Review 后就要上线，不确定的改动返工成本很高
6. **输出检查结果** — 向用户简要说明该模块的实现情况和 Review 结果
7. **收集用户反馈** — 如果用户指出问题，立即修复后再次 Review，直到通过

> 不要等所有模块做完才一起 Review，发现问题越早修复成本越低。

#### Review 反模式清单（禁止！）

以下是已发生过的 Review 失败案例，**必须引以为戒**：

| 反模式 | 真实案例 | 正确做法 |
|--------|---------|---------|
| **"看了但没看到"** | GeoServer craft 名 `"-XPZ"` 没改成 `"-PHC"`，Review 时声称检查了 API 路径 | 用 grep 搜索源工艺标识，机械化排查而不是目视扫描 |
| **"想当然推断"** | 看到 40 个文件都用 `/xpzMap/`，断定是公共服务不用改，实际后端有独立的 `/phcMap/` | 去后端代码验证，不能靠"别人也用"来推断 |
| **"改了注释当改了代码"** | 注释写"已修改"但实际值没改（`"-XPZ" // 已修改`） | Review 时看代码值，不看注释 |
| **"只改了一处漏了其他"** | 文件中有 3 处相同的 craft 值，只改了其中 1 处 | 用 replace_all 或 grep 全文确认所有出现位置 |
| **"文案没改"** | 导出文件名还写着"高压旋喷桩查询统计.xlsx" | 中文文案、文件名、提示语都要搜索替换 |

> **核心原则：** Review 不是"扫一眼觉得没问题"，而是**拿着检查清单一项一项执行**。移植类任务尤其需要机械化搜索，因为人眼（和 AI 眼）很容易对重复内容产生"一切正常"的错觉。

#### Level 2：全局 Review（所有模块实现完毕后）

当所有模块的 Level 1 Review 都通过后，做一次全局自检：

1. **跨模块一致性** — 检查各模块之间的字段命名、API 路径风格、交互逻辑是否一致
2. **遗漏检查** — 对照需求文档的目录/功能列表，确认是否有遗漏的模块或功能点
3. **工单评论复查** — 再次通读工单评论，确认所有变更都已反映到代码中
4. **全局残留扫描** — 对**所有改动过的文件**执行一次 grep，搜索源工艺的所有标识符，确保无遗漏
5. **推断验证** — 回顾整个实现过程中做的所有"推断"（如"这个是公共接口""这个值不用改"），逐一用代码或文档验证。**任何未经后端代码/文档验证的推断都是风险点**
6. **输出全局 Review 报告** — 包含各模块状态、发现的问题、建议修复方案

#### Level 3：用户验收 Review

全局 Review 完成后，将结果交给用户做最终验收：

1. **展示改动总览** — 列出所有模块的改动要点和当前状态
2. **提供测试指引** — 告诉用户如何运行/验证各功能
3. **收集反馈** — 用户测试后反馈问题，逐一修复
4. **循环迭代** — 修复 → Level 1 Review → 用户验证，直到用户确认通过

```
模块 A 实现 → L1 Review → 用户反馈 → 修复 → L1 通过
模块 B 实现 → L1 Review → 用户反馈 → 修复 → L1 通过
...
全部通过 → L2 全局 Review → L3 用户验收 → Phase 4.6 完整度检查
```

### 4.6 完整度检查

在主要功能实现完成后（或阶段性交付前），必须做一次**需求-实现全面对照检查**，确保不遗漏模块。

**检查步骤：**
1. **重读需求文档和所有图片** — 不要凭记忆，逐字逐图重新过一遍
2. **逐模块列表对照** — 按模块输出检查表，每项标注实现状态：

```
| 模块/需求项 | 需求说明 | 实现状态 | 备注 |
|------------|---------|---------|------|
| XXX功能    | 参照XX开发 | ✅ 已完成 / ⚠️ 部分完成 / ❌ 未完成 | 具体说明 |
```

3. **标注优先级** — 未完成项按优先级排序：
   - **P0** — 核心功能缺失，必须当前迭代完成
   - **P1** — 重要功能，本轮应完成
   - **P2** — 次要功能，可后续迭代
   - **P3** — 优化项/美化项
4. **同步给用户** — 将检查结果展示给用户，确认优先级和下一步计划

> **总分总思路：** Phase 3（总：Spec 规划）→ Phase 4（分：逐模块实现+L1 Review）→ Phase 4.5（Review 收敛）→ Phase 4.6（总：完整度复查）→ Phase 5（收尾）

---

## Phase 5: 收尾输出

### 5.1 生成日报

#### 5.1.1 日报生成流程

1. **检查本地个性化模板** — 先查看是否存在 `~/.cursor/skills/work-order-flow/local/daily-report-style.md`。如果存在，按该文件中的格式和风格生成日报
2. **无个性化模板** — 使用下方通用模板生成
3. **满意度确认** — 生成日报后**必须询问用户**："日报格式是否满意？如不满意，可以把你最近 7 天的日报发给我，我会学习你的风格并保存为个人模板"
4. **风格学习** — 如果用户提供了历史日报样本：
   - 分析样本中的格式模式（标号风格、标签用法、描述详细程度、项目名称写法、换行习惯等）
   - 提取风格规则，生成 `~/.cursor/skills/work-order-flow/local/daily-report-style.md`
   - 用学习到的风格重新生成日报，再次确认
5. **后续使用** — 一旦个性化模板存在，后续所有日报生成自动使用该模板，不再使用通用模板

#### 5.1.2 通用模板（未个性化时使用）

格式要求：

```
序号、【分类】-【项目】- 子系统/功能描述（工单编号：XXXXX）
```

**分类选项：** 功能开发 / 问题排查 / 问题修复 / 数据处理 / 对接 / 功能优化 / 会议讨论 / 其他

**示例：**
```
1、【功能开发】-【上海浦东四期】- 上海浦东四期项目平台PHC静压桩工艺需求分析及前端移植（工单编号：202605151017000013074）
2、【问题修复】-【上海浦东四期】- 数字沙盘强夯施工未正确显示实时位置，原因：代码逻辑判断错误（工单编号：202601130915000200088）
```

生成日报时需要包含：
- 正确的分类标签
- 准确的项目和子系统名称
- 简洁的工作内容描述（描述做了什么功能，不是"分析了需求"这种过程描述）
- 如果是修复类，附上原因说明
- 工单编号（无工单则写"无"）
- 每天 2-3 条，按时间跨度自动按天分组

#### 5.1.3 个性化模板文件格式

`local/daily-report-style.md` 的结构：

```markdown
# 日报风格配置

## 格式规则
- 标号风格：{1、/ 1. / - }
- 标签格式：{【分类】-【项目】/ 【分类】【项目】}
- 项目名称写法：{上海浦东四期 / 上海 / 浦东}
- 每日条目数：{2-3}
- 描述详细度：{简洁一句话 / 详细多句}
- 是否按天分组：{是/否，日期格式如 0521 / 05-21}

## 示例条目
（从用户样本中提取的 3-5 条典型日报，作为 few-shot 参考）
```

> **关键原则：** 个性化模板保存在**用户本地**（`local/` 子目录），不会被 git 提交到共享仓库。共享的 SKILL.md 只包含通用模板和学习机制的逻辑。`local/` 目录应添加到 `.gitignore`。

### 5.2 归档文档

将过程文档整理到需求文件夹中，确保：
- spec.md 中的任务状态已更新
- progress.md 记录了完整的执行过程
- 关键决策和踩坑记录保留

### 5.3 交付总结文档

**Phase 5 的最后一步**：生成一份**交付总结**，作为整个需求的"总"（对应 Phase 3 Spec 的"开头总"，这里是"收尾总"）。

单独输出给用户，内容包括：

```markdown
## 交付总结

### 需求概览
- 需求名称 / 工单编号 / 开始时间 / 完成时间

### 实现范围
| 模块 | 状态 | 关键改动文件 |
|------|------|------------|
| XXX  | ✅ 完成 | file1.vue, file2.vue |
| YYY  | ⚠️ 待后端 | — |

### 关键决策
- DR-001: ...
- DR-002: ...
（从 progress.md 决策记录中提取最重要的几条）

### 遗留事项
- 需运维配合的项
- 需后端配合的项
- 后续迭代的项

### 部署/测试注意事项
- 前端需要重新构建
- 需要配置 XX 环境变量
- 需要创建 XX 数据库表/GeoServer 工作空间
```

> **目的：** 让任何人（包括未来的 AI、其他开发者、运维）拿到这份文档就能了解"做了什么、还差什么、怎么上线"。

---

## 快速通道（小需求/Bug修复）

对于简单的 bug 修复或小调整，简化流程：

```
识别问题 → 定位代码 → 确认方案 → 修复 → 生成日报条目
```

不需要创建需求文件夹和 Spec，但仍然需要：
1. 确认理解正确
2. 修复前说明方案
3. 最后提供日报条目

---

## 批量零散优化模式

当用户一次性提出多个分散在不同位置的小优化点时，使用此模式。**核心问题：上下文污染**——多个零散问题混在一起导致信息错乱、改错文件。

### 处理流程

```
收集全部优化点 → 总体分析（关联/冲突/顺序） → 写入临时 TODO → 逐个隔离执行 → 汇总
```

### 详细步骤

**Step 1: 总体鸟瞰**

收到全部优化点后，先做全局分析，**不动手写代码**：
1. 列出所有优化点，逐个评估影响范围
2. 检查**关联性**——是否有两个优化点涉及同一文件或同一逻辑？
3. 检查**冲突性**——是否有两个优化点可能互相矛盾？
4. 确定**执行顺序**——有依赖关系的排前面，无依赖的按影响范围从小到大

**Step 2: 写入 TODO 清单**

将分析结果写入 IDE 的 TODO 工具（或 progress.md），格式：

```
| # | 优化点 | 涉及文件 | 关联 | 优先级 | 状态 |
|---|-------|---------|------|-------|------|
| 1 | XXX   | a.vue   | 无   | P1    | 待处理 |
| 2 | YYY   | b.vue, c.vue | 与#3共享文件 | P1 | 待处理 |
| 3 | ZZZ   | c.vue   | 与#2共享文件 | P2 | 待处理 |
```

**Step 3: 隔离执行（关键！）**

逐个处理每个优化点，**严格隔离上下文**：
- 开始一个优化点时，**只关注该优化点所需的文件和逻辑**
- 完成后，标记为已完成
- 进入下一个优化点时，**不携带上一个的代码上下文**（但保持 TODO 清单的全局视角）
- 如果发现当前优化点与已完成的有冲突，回到 TODO 清单重新评估

**Step 4: 完成后汇总**

所有优化点处理完后：
1. 回到 TODO 清单，确认全部完成
2. 统一做一次编译检查
3. 简要总结每个改动

> **上帝视角原则：** TODO 清单是"上帝视角"——始终知道全局进度；但在执行单个任务时，视野缩窄到只看当前任务。就像一个项目经理知道所有任务的进度，但让每个开发者只专注自己的票。

---

## 附加说明

### 提交建议机制

在以下时机，AI 应**主动建议**用户进行 git commit，并给出中文 commit 信息草稿：

**触发时机：**
| 场景 | 建议话术 |
|------|---------|
| 一个独立模块/功能完成且编译通过 | "当前改动编译通过，建议先提交一次。commit 信息建议：`...`" |
| 批量零散优化全部完成 | "所有优化点处理完毕，建议统一提交。commit 信息建议：`...`" |
| stash 取出并合并成功 | "stash 合并无冲突，建议提交合并结果。commit 信息建议：`...`" |
| 当前改动文件数 ≥ 5 且有一段时间没提交 | "当前有 N 个文件改动未提交，建议先保存一个 checkpoint" |
| Phase 4 一轮迭代完成（L1 Review 通过） | "本轮迭代完成，建议提交" |
| Phase 5 收尾时 | "最终状态确认，建议做一次收尾提交" |

**commit 信息规范：**
- **语言：中文**
- **格式：** `[类型] 简要描述`
- **类型选项：** `feat`（功能）、`fix`（修复）、`style`（样式）、`refactor`（重构）、`chore`（杂务）
- **描述原则：描述需求本身做了什么，而不是开发者做了什么操作**
  - ❌ `chore: 恢复摊铺工艺暂存代码`（"恢复"是操作，不是需求）
  - ✅ `feat: 摊铺工艺单元管理及监控功能开发`（描述功能本身）
  - ❌ `fix: 修改了XPZ为PHC`（描述动作）
  - ✅ `fix: PHC实时监控地图图层craft名称修正`（描述问题/结果）
- **示例：**
  - `feat: PHC静压桩实时监控页面移植`
  - `fix: 单元管理PHC右侧空白问题（kind类型转换）`
  - `style: 隐藏所有工艺数据报表的导出按钮`
  - `feat: 摊铺工艺单元管理及监控功能`

**多意图改动的分组提交：**

当一次会话产生了多个不同意图的改动（如"隐藏按钮" + "合并 stash" + "沙盘优化"），**不要合并为一次提交**。应按意图分组：

1. 列出所有改动文件，按意图分组
2. 给每组一个独立的 commit 信息
3. 提供三种选择给用户：
   - **A. 让 AI 执行** — AI 按分组逐次 `git add` + `git commit`
   - **B. 用户手动** — AI 给出每组的文件列表和 commit 命令，用户自己执行
   - **C. 先不提交** — 不再提起，用户自行决定

用户选择后照做即可，**不要追问**。

**严格约束：**
- 未经用户选择，**绝不执行** commit/push
- 用户选 C 后，不再主动提起提交话题
- 提交建议只出现一次，不重复催促

**反面案例（提交踩坑记录）：**
- **git stash apply 后 staged 状态污染：** `git stash apply` 可能将文件放入 staged 区域。如果之后对其他文件 `git add`，staged 文件会一起被提交。**解决：** 分组提交前，先 `git reset HEAD .` 清空 staged，再逐组 `git add` 指定文件。
- **必须验证提交内容：** 每次 `git commit` 后立刻 `git show --stat HEAD` 检查实际提交的文件数和文件列表是否符合预期。发现混入不相关文件时，立刻 `git reset --soft HEAD~1` 拆分重提。

### 关于文档解析的局限性

- Word 中的复杂表格、SmartArt、图表可能解析不完美
- 扫描版 PDF（图片型）无法直接提取文字，需 OCR
- 遇到解析问题时，请用户手动补充关键内容

### 关于团队复用与分发

#### 定位：平台无关的通用 Skills

本 Skill 的核心是一份 SKILL.md 文件和配套脚本，**不绑定任何特定 AI 编码平台**。它本质上就是一个**文件夹**——任何能读取文件、执行指令的 AI Agent 都可以使用。

**保底机制：** 即使不使用任何包管理器或平台特性，只要把 `work-order-flow/` 文件夹放到 AI Agent 能访问的路径下，就能正常工作。

#### 目录结构

```
work-order-flow/
├── SKILL.md              # 核心工作流定义（唯一必需文件）
├── CHANGELOG.md          # 版本变更记录（必须维护）
├── scripts/
│   ├── parse_doc.py      # 文档解析脚本
│   └── requirements.txt  # Python 依赖
├── local/                # 个人配置（不纳入版本控制）
│   ├── daily-report-style.md   # 日报个性化模板
│   ├── preferences.md          # 使用偏好
│   └── version.txt             # 本地版本号记录
├── README.md
└── .gitignore            # 排除 local/
```

#### 平台适配

| 平台 | 个人级路径 | 项目级路径 | 备注 |
|------|-----------|-----------|------|
| **Cursor** | `~/.cursor/skills/work-order-flow/` | `.cursor/skills/work-order-flow/` | 可选封装为 Plugin |
| **Claude Code** | `~/.claude/skills/work-order-flow/` 或 `CLAUDE.md` 引用 | `.claude/skills/work-order-flow/` | 也可在 CLAUDE.md 中 include |
| **Codex / GPT Agent** | 项目 `.agents/skills/work-order-flow/` | 同左 | 或将内容嵌入 system prompt |
| **Tare / Qoder / Windsurf** | 按各平台 skills 目录放置 | 项目根目录对应路径 | 参考各平台文档 |
| **通用/未知平台** | 任意路径 + 在对话中告知 AI 读取 | 项目内任意位置 | **保底方案** |

> **保底方案**：如果平台不支持 skills 机制，直接告诉 AI "请读取 `{路径}/SKILL.md` 并按其中的工作流执行"即可。

#### 分发方式（推荐 → 保底）

**方式一：npx 一键安装（推荐，最低门槛）**

大部分用户有 Node.js 环境，直接运行：

```bash
# 自动检测平台并安装到对应 skills 目录
npx @sw-liux/work-order-flow install

# 指定平台
npx @sw-liux/work-order-flow install --platform cursor
npx @sw-liux/work-order-flow install --platform claude-code
npx @sw-liux/work-order-flow install --platform codex

# 指定自定义路径
npx @sw-liux/work-order-flow install --path ~/my-skills/work-order-flow

# 更新到最新版本
npx @sw-liux/work-order-flow update
```

安装脚本逻辑：
1. 检测当前环境（有无 `.cursor/`、`.claude/` 等）
2. 自动确定目标路径
3. 下载 SKILL.md + scripts/ + CHANGELOG.md
4. 创建 `local/` 目录和 `.gitignore`
5. 打印安装结果和使用说明

> **包名为占位符**——发布时替换为实际 npm 包名（如 `@你的用户名/work-order-flow`）。

**方式二：Git 仓库（推荐给团队协作场景）**

```bash
# 首次安装
git clone https://github.com/your-org/work-order-flow.git ~/你的AI平台skills路径/work-order-flow

# 更新
cd ~/你的AI平台skills路径/work-order-flow && git pull
```

适用于需要自定义修改、参与贡献的用户。

**方式三：直接复制（保底）**

对于不使用 npm 也不使用 Git 的用户：
1. 从 GitHub 页面下载 ZIP 或直接下载 `SKILL.md` 和 `scripts/` 文件夹
2. 放到任意位置
3. 在 AI 对话中手动引用即可（告诉 AI "请读取 {路径}/SKILL.md"）

#### 更新与变更日志

**CHANGELOG.md 是强制的。** 任何对 SKILL.md 的修改必须同步更新 CHANGELOG.md：

```markdown
# Changelog

## [1.6.0] - 2026-05-23
### Added
- 日报个性化机制：支持学习用户风格并保存本地模板
- Skills 分发机制：平台无关的三级分发方案（Git/平台适配/直接复制）
- 反馈优化机制：工作流缺陷 vs 个人偏好二分法
- 版本更新提醒机制

### Changed
- 定位从 Cursor 专属调整为平台无关的通用 Skills
```

**更新提醒机制：**

1. **版本号管理** — SKILL.md frontmatter 包含 `version` 和 `last_updated` 字段
2. **本地版本记录** — AI 首次使用时在 `local/version.txt` 记录当前版本号
3. **版本检查** — 在 Phase -1（会话恢复）或首次使用时：
   - 对比 `local/version.txt` 与 SKILL.md 的 `version`
   - 版本不一致时读取 CHANGELOG.md，提示用户变更摘要
   - 更新 `local/version.txt`
4. **原则**：被动感知（本地文件对比），不联网轮询。离线也能用

### 解析脚本位置

解析脚本 `parse_doc.py` 位于本 Skill 目录下的 `scripts/` 文件夹中。
使用前安装依赖：`pip install -r scripts/requirements.txt`

平台无关——任何能执行 Python 的环境都可以运行。

### 反馈优化与个性化机制

#### 触发策略：低频精准触发

**核心原则：** 反馈收集不能干扰正常开发。在用户专注写代码时减少触发，在用户情绪信号明确时精准触发。

| 场景 | 是否触发 | 原因 |
|------|---------|------|
| 用户正常开发中 | **不触发** | 避免打断工作上下文 |
| 用户明确表达不满（"这样不好"、"太麻烦了"） | **触发** | 情绪信号明确 |
| 用户表达满意（"不错"、"这样挺好"） | **轻量触发** | 收集正面案例（简短记录，不展开讨论） |
| Phase 5 收尾时 | **可选触发** | 任务完成，不影响工作上下文 |
| 同一会话中已触发过 | **不再触发** | 避免反复打断 |

**触发后的行为：**
- **不满触发**：立刻暂停，进入分析流程（见下方）。分析完后简洁处理（2-3 句话），然后**立刻回到正常工作**
- **满意触发**：不打断，在当前回复末尾简单记一笔："已记录该模式为良好实践"。后续汇总时再整理
- **收尾触发**：在 Phase 5 日报后，附一句"本次工作流有什么想反馈的吗？"。没有就跳过

#### 分类判断：工作流缺陷 vs 个人偏好

| 维度 | 工作流缺陷 | 个人偏好 |
|------|-----------|---------|
| **特征** | 其他人也会遇到同样问题 | 只有该用户特别在意 |
| **示例** | "Review 没发现残留问题"→Review 规则不完善 | "日报格式不是我习惯的"→个人风格 |
| **示例** | "你做完一步不主动说下一步"→阶段推进规则缺失 | "每次都问我太烦了"→交互频率偏好 |
| **示例** | "移植残留一直没被发现"→扫描机制缺失 | "我喜欢先做后端再做前端"→执行顺序偏好 |
| **处理** | 优化共享工作流 | 保存到本地个性化配置 |

**判断不确定时**，简短问用户："这个是你的个人习惯，还是你觉得大家都应该这样？"

#### 工作流缺陷 → 反馈提交

当 AI 判断某个问题是工作流通用缺陷（而非个人偏好）时，应**主动帮用户完成反馈提交**，最大程度减少用户的操作负担。

**核心原则：用户只需要说"行"或"不行"，剩下的 AI 全做。**

**Step 1：AI 整理反馈内容**

AI 在内部整理结构化反馈（不需要展示完整表单给用户）：

```markdown
## 工作流优化反馈
- 阶段：Phase X.Y
- 问题：{一句话描述}
- 期望：{一句话描述}
- 建议修复：{方案}
- 触发场景：{用户当时在做什么导致发现这个问题}
```

**Step 2：AI 主动告知用户并请求确认**

AI 用一句话告诉用户接下来要做什么，**不要解释 Git 流程细节**：

```
这个问题是工作流的通用缺陷，其他人也可能遇到。
我帮你提交一个反馈到工作流仓库，维护者审阅后会纳入优化。要提交吗？
```

**Step 3：根据环境执行**

用户确认后，按环境选择路径：

**路径 A：有 `gh` CLI 或 Git 环境**

AI 直接执行，用户无需做任何操作：

```bash
# AI 自动执行（用户只需等结果）
gh issue create \
  --repo "{维护者仓库地址}" \
  --title "[反馈] Phase X.Y - {问题一句话描述}" \
  --body "$(cat <<'EOF'
## 问题描述
{问题详细描述}

## 触发场景
{用户当时在做什么}

## 期望行为
{应该怎样}

## 建议修复
{AI 的修复方案}

## 环境信息
- Skills 版本：{version}
- 平台：{Cursor/Claude Code/...}
EOF
)"
```

执行后告诉用户："已提交，Issue 链接：{URL}。维护者会审阅处理。"

如果 AI 有能力直接修复（如修改 SKILL.md 中的规则），可以追加提一个 PR：

```bash
# 可选：AI 直接提 PR 修复
git checkout -b fix/phase-x-y-issue-desc
# ... 修改 SKILL.md + CHANGELOG.md ...
gh pr create \
  --repo "{维护者仓库地址}" \
  --title "[Fix] Phase X.Y - {修复描述}" \
  --body "Fixes #{issue号}"
```

**路径 B：无 Git 环境（保底）**

AI 将反馈整理为一段可复制的文本，并告诉用户：

```
我把反馈整理好了，你可以通过以下任一方式发给维护者：
1. 复制下面的内容，发到维护者的{联系方式}
2. 打开 {仓库URL}/issues/new，把内容粘贴进去
3. 我先保存到本地 local/pending-feedback.md，等你有条件时再提交

你选哪个？
```

**路径 C：用户拒绝提交**

尊重用户意愿，保存到 `local/pending-feedback.md` 暂存，不再追问。

> **关键：整个过程中用户只需做两个决策：1）要不要提交；2）选哪种方式。其他所有操作由 AI 完成。不要让用户去学 Git 命令、写 Issue 模板、填表单。**

#### 个人偏好 → 本地个性化

保存到 `local/preferences.md`：

```markdown
# 个人使用偏好

## 交互偏好
- 阶段推进方式：{自动推进不问 / 每次确认}
- Review 提醒频率：{每个模块 / 只在关键节点}
- 提交建议频率：{主动建议 / 只在用户要求时}

## 执行偏好  
- 优先执行顺序：{前端优先 / 后端优先 / 按文档顺序}
- 文档更新频率：{实时 / 阶段完成后}

## 日报偏好
- 参见 daily-report-style.md

## 良好实践记录
- {从用户满意反馈中提取的正面模式}

## 其他
- {从使用过程中学习到的个性化规则}
```

AI 在 Phase -1（会话恢复）时读取 `local/preferences.md`，自动应用个人偏好。

#### 反馈闭环

```
用户情绪信号 → AI 判断是否触发（低频精准）
  │
  ├→ 满意 → 简要记录到 local/preferences.md 的"良好实践"
  │
  └→ 不满 → 判断类型
       ├→ 工作流缺陷 → 收集反馈 → 路径A(Git)/路径B(保底) → 版本更新
       └→ 个人偏好 → 写入 local/preferences.md → 后续自动应用
```

> **核心原则：**
> 1. **低侵入**——正常工作时不打断，情绪信号时才触发
> 2. **双通道**——公共优化让所有人受益，私人定制让个人舒适
> 3. **有保底**——没 Git 也能反馈，`local/` 目录永远不会被提交到共享仓库

### 未来自动化方向

当前工作流为手动触发模式。后续可对接：
1. 钉钉机器人 webhook → 自动接收工单
2. 文档自动解析管线
3. 自动打包部署流程
4. 日报自动汇总推送

---

## 维护者信息

```yaml
maintainer: SW-Liux
contact: liuxin@swinfo.com.cn
repo: https://github.com/SW-Liux/work-order-flow
```

> 使用者反馈工作流问题时，可直接到 [GitHub Issues](https://github.com/SW-Liux/work-order-flow/issues) 提交，或发邮件到上述联系方式。
